# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# جلسات واتساب المحلية (لا تُرفع أبداً — كبيرة وتحوي جلسات)
.wwebjs_auth/
.wwebjs_cache/

# ملفات تشغيل/سجلات مؤقتة
_srv.log
*.log

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# إعدادات Claude المحلية
.claude/

# نسخةُ تراجعِ وسوم «الكروت الوهميّة» الكاذبة — بياناتُ إنتاجٍ لا تُودَع
phantom-flags-backup-*.json

# تقريرُ فحصِ الكروت الوهميّة — بياناتُ إنتاجٍ لا تُودَع
phantom-verification-report.json

# ملفُّ التعافي — لا يُودَع ما دام المستودعُ عامّاً (شرطُ محمد)
docs/RECOVERY.md
# سكربتاتُ فحصٍ مؤقّتة
_q.mjs
_qr.mjs
_fix.cjs
_new.txt
_home-test.html
