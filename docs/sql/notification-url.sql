-- ═════ 🔗 وجهةُ الإشعار عند نقره — عمودٌ واحدٌ يُضاف (2026-08-24) ═════
--
-- 🔴 السبب: بلاغُ محمد «الإشعارُ يظهر ولا يمكن الضغط عليه». وكان `notify()` يستقبل `url`
--   ويُمرّره إلى دفعة الهاتف **ولا يخزّنه**، فجرسُ الإشعارات يبني وجهةَ النقر من خريطةٍ
--   يدويّةٍ في الواجهة تعرف **نوعَين من أحدَ عشر** — فكلُّ نوعٍ جديدٍ يولد غيرَ قابلٍ للنقر.
--
-- ✅ إضافةٌ محضة: عمودٌ اختياريٌّ لا يمسّ صفّاً قائماً ولا سلوكاً قائماً. والكودُ يعمل قبل
--   لصقه وبعده (يحاول بالعمود، وإن غاب أعاد الكتابةَ بالحقول القديمة) — فلا نافذةَ تعطّل.
--
-- 📋 يُلصق في: Railway ← Postgres ← Console

ALTER TABLE notifications ADD COLUMN IF NOT EXISTS "url" TEXT;
