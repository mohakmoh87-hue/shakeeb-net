// قوالب الرسائل: النصوص الافتراضية + جلب «القالب الفعّال» لوكيل معيّن.
// النصوص أدناه حرفية (بما فيها نجوم واتساب * للخط العريض) — لا تُعدَّل صياغتها.
import { prisma } from "./prisma";

// صف علامة لكل وكيل: زُرعت النصوص الافتراضية له مرة واحدة (يُستثنى من كل القوائم)
export const SEED_MARK = "__seed_ar_v1";

// ═════ 📦 «تذكير قبل الانتهاء **حسب الباقة**» (طلبُ محمد 2026-08-21) ═════
// نصُّه: «قالبٌ لكلّ باقةٍ موجودة، وأيُّ باقةٍ تُضاف مستقبلاً يصير لها قالبٌ فيه.
//  ويُرسَل للمشترك قبل انتهائه **حسب باقته**، ومن ليس لديه باقةٌ لا يصله شيء.
//  وتفعيلُه يُلغي تفعيلَ القالب القديم تلقائيّاً والعكس — لا يجتمعان أبداً».
// 🔑 والتخزينُ في `sms_templates` نفسِه فيرث مجّاناً: عزلَ الوكيل · تخصيصَ المكتب ·
//    الصورةَ · سقفَ حجمها · واسترجاعَ المساحة. `expiringByPkg` مفتاحُ الوضع (enable)،
//    و`expiringPkg:{packageId}` نصُّ كلّ باقةٍ على حدة.
export const EXPIRING_BY_PKG = "expiringByPkg";
export const expiringPkgType = (packageId: number): string => `expiringPkg:${packageId}`;

/** أوضاعُ تذكير الانتهاء: القديمُ العامّ أم الجديدُ حسب الباقة (لا يجتمعان). */
export async function expiringByPkgOn(agentId: number | null, towerId?: number | null): Promise<boolean> {
  // سلَّمُ الأولويّة نفسُه: صفُّ المكتب يغلب صفَّ الوكيل — فمكتبٌ يختار وضعَه.
  if (towerId != null) {
    const o = await prisma.smsTemplate.findFirst({
      where: { type: EXPIRING_BY_PKG, agentId: agentId ?? -1, towerId }, select: { enable: true },
    });
    if (o) return o.enable === "1";
  }
  const a = await prisma.smsTemplate.findFirst({
    where: { type: EXPIRING_BY_PKG, agentId: agentId ?? -1, towerId: null }, select: { enable: true },
  });
  return a?.enable === "1";
}

// أنواع القوالب المربوطة بالأحداث التلقائية/شبه التلقائية
export const EVENT_TYPES = [
  "activation", // تفعيل الاشتراك (تلقائي عند التفعيل)
  "expiring", // تذكير قبل الانتهاء (المجدول اليومي)
  "debtPaid", // تسديد دين (تلقائي عند تسديد دفعة)
  "debts", // مطالبة بالديون (يدوي من صفحة الديون)
  "maintenance", // الصيانة/التنصيب (عند إنجاز الفني)
  "cardRaised", // رُفعت بطاقةٌ للمشترك (من البرنامج أو أودو) — البند ٧
  "expiredSince", // انتهى اشتراكُه منذ N يوماً (المجدول اليوميّ) — البند ٤-أ
  "selfActivated", // فعّل اشتراكَه بنفسه لا بالمكتب (تُكتشَف بالمزامنة) — البند ٤-ب، واسمُه للمدير «تفعيلات خارجية»
  "externalInstall", // تنصيبٌ/إعادةٌ من الشركة رُصدت بسجلّ المزامنة — «تنصيبات خارجية» (طلب محمد 2026-08-20)
  "reward", // منح المكافأة
  "rewardUsed", // استخدام المكافأة
  "subSummary", // وصل ملخص الاشتراك (زر «ارسال ملخص» في صفحة المشتركين)
  "noAnswer", // «ميجاوب»: الفني اتصل بالمشترك ولم يجب (البطاقة تبقى بمكانها)
  "loan", // قرض فزعة (عند منح القرض للمشترك)
  "other", // قديم: تصنيف عام
] as const;

// النصوص الافتراضية الثلاثة (تُستعمل عند غياب نص محفوظ، وتُزرع للوكلاء مرة واحدة)
export const DEFAULT_TEMPLATES: Record<string, string> = {
  activation: `*تم تفعيل اشتراك الانترنت الخاص بكم بنجاح*
نوع الباقة : {نوع_الباقة}
البطاقة : {البطاقة}
اسم المستخدم : {اسم_المستخدم}
اسم المشترك : {اسم_المشترك}
مبلغ الاشتراك : *{مبلغ_الاشتراك}*
تم استلام مبلغ قدره : *{المبلغ_المستلم}*
والمبلغ المتبقي : *{المبلغ_المتبقي}*
اجمالي الديون : *{اجمالي_الديون}*
سينتهي الاشتراك في الساعة الخامسة مساءا بتاريخ : {تاريخ_الانتهاء}`,
  expiring: `*تذكير : اشتراك الانترنت الخاص بكم قارب على الانتهاء*
اسم المشترك : {اسم_المشترك}
اسم المستخدم : {اسم_المستخدم}
نوع الباقة : {نوع_الباقة}
اجمالي الديون : *{اجمالي_الديون}*
سينتهي الاشتراك في الساعة الخامسة مساءا بتاريخ : {تاريخ_الانتهاء}
يرجى التجديد قبل انتهاء المدة لتجنب انقطاع الخدمة`,
  debtPaid: `*تم تسديد دفعة من الديون بنجاح*
اسم المشترك : {اسم_المشترك}
تم استلام مبلغ قدره : *{المبلغ_المستلم}*
اجمالي الديون : *{اجمالي_الديون}*
شكرا لتعاملكم معنا`,
  noAnswer: `*محاولة تواصل من {office}* 📞
عزيزي المشترك {اسم_المشترك}
حاول الفني {technician} الاتصال بكم بخصوص طلبكم «{kind}» ولم نتمكن من الوصول إليكم.
بطاقتكم ما زالت محفوظة لدينا وسنعاود المحاولة بأقرب وقت.
يسعدنا اتصالكم بنا لتحديد الوقت المناسب لكم 🌹
{office}`,
  subSummary: `*ملخص اشتراك الانترنت الخاص بكم*
اسم المشترك : {اسم_المشترك}
اسم المستخدم : {اسم_المستخدم}
نوع الباقة : {نوع_الباقة}
مبلغ الاشتراك : *{مبلغ_الاشتراك}*
اجمالي الديون : *{اجمالي_الديون}*
سينتهي الاشتراك في الساعة الخامسة مساءا بتاريخ : {تاريخ_الانتهاء}
{office}`,
  loan: `*تم منحكم قرض فزعة من {المكتب}*
عزيزي المشترك {الاسم}
تم تمديد اشتراككم لمدة *30 يوماً* كقرض.
اسم المستخدم : {اليوزر}
مبلغ القرض : *{المبلغ}*
ينتهي بتاريخ : {تاريخ_الانتهاء}
نرجو تسديد المبلغ عند التجديد القادم. شكراً لثقتكم بنا 🌹`,

  // البند ٤-أ · «انتهى منذ N يوم» — رسالةٌ واحدةٌ لكلّ انتهاء، لا كلَّ يوم
  expiredSince: `*انتهى اشتراكُ الانترنت الخاصّ بكم*
اسم المشترك : {اسم_المشترك}
اسم المستخدم : {اسم_المستخدم}
نوع الباقة : {نوع_الباقة}
تاريخ الانتهاء : {تاريخ_الانتهاء}
اجمالي الديون : *{اجمالي_الديون}*
يرجى التجديد لإعادة الخدمة. نحن في خدمتكم 🌹
{office}`,

  // البند ٤-ب · «فعّل بنفسه» — يُكتشَف بالمزامنة (المُفعِّلُ في الساس ليس حسابَ المكتب)
  // ⚠️ **بلا تاريخ انتهاءٍ أبداً** (قرار محمد 2026-08-21 — قالبا سجلّ المزامنة كلاهما):
  // تاريخُ لحظةِ الرصد قد لا يكون التاريخَ النهائيَّ فتصل رسالةٌ بتاريخٍ خاطئ.
  selfActivated: `*تم تفعيل اشتراككم بنجاح*
اسم المشترك : {اسم_المشترك}
اسم المستخدم : {اسم_المستخدم}
نوع الباقة : {نوع_الباقة}
اجمالي الديون : *{اجمالي_الديون}*
شكراً لتعاملكم معنا 🌹
{office}`,

  // 📋 سجلّ المزامنة · «تنصيبات خارجية» — تنصيبٌ/إعادةٌ من الشركة (طلبُ محمد 2026-08-20)
  // ⚠️ **بلا تاريخ انتهاءٍ أبداً** (قراره 2026-08-21): أوّلُ رسالةٍ كانت ستحمل تاريخَ
  // العرض المؤقّت (١٠ أيّام) قبل أن تضيف الشركةُ أيّامَه الحقيقيّة — تاريخٌ خاطئٌ حتماً.
  externalInstall: `*أهلاً بكم في {office}*
عزيزي المشترك {اسم_المشترك}
تم تنصيب اشتراك الانترنت الخاص بكم بنجاح
اسم المستخدم : {اسم_المستخدم}
نوع الباقة : {نوع_الباقة}
نتمنى لكم خدمة ممتعة 🌹
{office}`,

  // البند ٧ · رُفعت بطاقةٌ للمشترك — من البرنامج أو من أودو (طلبُ محمد 2026-08-13)
  cardRaised: `*تم استلام طلبكم في {المكتب}*
عزيزي المشترك {الاسم}
تم تسجيل طلبكم : *{العملية}*
اسم المستخدم : {اليوزر}
سيتواصل معكم الفنيّ في أقرب وقت. شكراً لصبركم 🌹`,
};

// نص القالب الفعّال: قالب المكتب المخصّص أولاً (إن مُرّر towerId ووُجد)، ثم قالب الوكيل
// العام (towerId فارغ)، ثم الافتراضي. يرجع null إن كان القالب الغالب معطَّلاً (enable = "0").
export async function getEffectiveTemplate(type: string, agentId: number | null, towerId?: number | null): Promise<string | null> {
  if (towerId != null) {
    const o = await prisma.smsTemplate.findFirst({ where: { type, agentId: agentId ?? -1, towerId } });
    if (o) {
      if (o.enable === "0") return null; // معطَّل لهذا المكتب تحديداً
      const text = o.text?.trim();
      if (text) return text;
      // نص المكتب فارغ ⇒ يسقط لقالب الوكيل العام
    }
  }
  const t = await prisma.smsTemplate.findFirst({ where: { type, agentId: agentId ?? -1, towerId: null } });
  if (t?.enable === "0") return null;
  const text = t?.text?.trim();
  return text || DEFAULT_TEMPLATES[type] || null;
}

// ═════ البند ٣ · القالبُ الفعّالُ **بنصِّه وصورته** (طلبُ محمد 2026-08-13) ═════
// دالّةٌ ثانيةٌ لا تعديلٌ للأولى: `getEffectiveTemplate` يناديها **أكثرُ من عشرين موضعاً**
// وتغييرُ نوعِ رجوعها يمسّها كلَّها — والمخاطرةُ بلا مقابل. فمَن يريد الصورةَ ينادي هذه.
//
// 🔑 والصورةُ تتبع **نفسَ سلَّم الأولويّة** الذي يتبعه النصّ: قالبُ المكتب ← قالبُ الوكيل.
//   لكنّها **مستقلّةٌ عن النصّ في السقوط**: مكتبٌ كتب نصّاً بلا صورةٍ يأخذ نصَّه وصورةَ
//   الوكيل — وهو الأنفعُ عملاً (شعارُ الوكيل واحدٌ ونصُّ المكتب يخصّه).
export async function getEffectiveTemplateFull(
  type: string, agentId: number | null, towerId?: number | null,
): Promise<{ text: string; image: string | null } | null> {
  const text = await getEffectiveTemplate(type, agentId, towerId);
  if (!text) return null; // معطَّلٌ أو غائبٌ ⇒ لا إرسالَ أصلاً
  let image: string | null = null;
  if (towerId != null) {
    const o = await prisma.smsTemplate.findFirst({
      where: { type, agentId: agentId ?? -1, towerId }, select: { image: true },
    });
    image = o?.image?.trim() || null;
  }
  if (!image) {
    const t = await prisma.smsTemplate.findFirst({
      where: { type, agentId: agentId ?? -1, towerId: null }, select: { image: true },
    });
    image = t?.image?.trim() || null;
  }
  return { text, image };
}
