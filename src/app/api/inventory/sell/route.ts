import { NextResponse } from "next/server";

export const dynamic = "force-dynamic";

// ===== البيع من المخزن أُلغي نهائياً (قرار محمد 2026-08-04) =====
// السبب: هذا المسار كان يسجّل حركة مالية sourceType="sale" **بلا فاتورة**، فلا يظهر
// البيع في سجل وصولات المبيع (يقرأ الفواتير)، ولا في المصروفات والمقبوضات (تعرض
// اليدوي فقط)، ولا في سجل التفعيلات — بينما يدخل التقرير اليومي. فصار مالٌ محسوب
// بلا وصلٍ يُرى أو يُحذف (حادثة UPS بـ25,000 في الشهداء 31 تموز، ومعها ٧ مبيعات أخرى).
// البيع الآن حصراً من «فاتورة مبيع»: تُنشئ فاتورة برقم وبنود، تُنقص المخزن، ولها وصل
// يُطبع ويُحذف. والمخزن للتعديل والإضافة والترحيل وذمم الفنيين فقط.
export async function POST() {
  return NextResponse.json(
    { error: "البيع من المخزن أُلغي — سجّل البيع من «فاتورة مبيع» ليكون له وصل يُطبع ويُحذف" },
    { status: 410 },
  );
}
